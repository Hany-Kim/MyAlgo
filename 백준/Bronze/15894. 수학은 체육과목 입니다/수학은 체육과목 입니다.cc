#include <iostream>
using namespace std;

long long N;

void sol() {
	cin >> N;
	long long ans = N * 4;

	cout << ans;
}

int main() {
	//freopen("input.txt", "r", stdin);
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	sol();
	return 0;
}