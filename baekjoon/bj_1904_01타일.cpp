#include <iostream>
#include <vector>
#include <string>
using namespace std;

string kind[2] = { "1", "00" };
int N;
vector<string> path;

void sol() {
	cin >> N;
}

int main() {
	freopen_s(new FILE*, "input.txt", "r", stdin);
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	sol();
	return 0;
}